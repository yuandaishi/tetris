//1.设置难度级别(下落速度)，倍数关系。传入的是1倍的速度
//2.创建下落块对象，包裹竖线，方块，Z型（两个方向），L型（两个方向），凸型
//3.得分机制，一次性消除一行，10分，2行30分，3行60分，4行100分
//4.出现图形预示
//5.键盘箭头A,D控制左右移动，S控制快速下滑。W控制变形,空格控制暂停
//6.得分区显示
//7.顶到顶部时失败，弹出失败提示。
//8.开始按钮
//9.消除机制。下落的滑块不能再移动时，判断填满的行消除
//10.区域划分成每个小空格，设置条件，有利于后续的拓展。开始con为0，有图形填进去的时候，为1.
//11.con为1的时候，为实体，为0的时候，是虚拟的
//12.每个小方块组成图形，图形有下落的速度和横向移动的速度、旋转角度，小方块的速度继承图形的速度，判断每个小方块是否碰撞，则可以判断大图形是否碰撞
//13.小方块边长，canvas有多少行，多少列
//14.重新绘制，消除的不绘制，消除上方的往下移动一格
//15.方块和方块之间有边长的间隔。
//16.初始坐标相等，则把con设置为1，为了后续的判断
//17.canvas一像素问题
//18.滑块到达底部或者和底部已有滑块碰撞时，新的滑块产生
//19.感觉要死了，好复杂
//20.每种图形的旋转中心都不一样，草
//21.不要做旋转计算，还是放到图形设计中比较简单，如果做旋转的话，会遇到很复杂的情况
//22.是否碰到左，右边界，上下边界，需要分别判断，上边界是滑块停止之后判断
//23.判断碰撞，向左，右，下，移动的时候，分别判断和con为1的空格是否接触（应该有更好的判断方法）
//24.点击的时候，依靠数组坐标加1变形，到达最大值的时候，坐标重新变成0，与左右碰撞的时候，有可能不能变形
//25.所创建的图形。始终在一个4x4的区域里，这一点在计算的时候要注意
//26.分数越高，级别越高。玩的过程中不能选择初始级别，暂停也不行
//27.enter键开始执行
//28.下落的滑块发生碰撞的时候，则产生新的下落滑块，但是原下落滑块移除，下落的滑块始终只有一个
//29.不仅仅要判断是否碰到左右边界，还要判断左右是否有con为1的虚拟滑块
//30.判断左右虚拟方块con值的时候，加1减一的时候，有可能到上一行下一行，但是这种情况就靠边了，所以这样判断还是对的\
//31.判断右边con的时候，先判断右边虚拟滑块是否存在，也就是是否大于虚拟滑块数，不然会出现不存在闪动的情况，左边就不用判断，一开始没人能瞬间把滑块移动到最左边